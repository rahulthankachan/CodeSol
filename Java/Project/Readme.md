## Project - SimpleBank


###Scanner

*  Make sure to use the scanner class as in the code sample
*  Make sure the **import is correct** also make sure that the **System.in** is passed properly



````
import java.util.Scanner

Scanner scanner = new Scanner(System.in)

````

Also  this command can be use `scanner.nextLine()`

###Trim()

To trim down the whitespaces **trim()** function can be used


###str.equalsignoreCase("String to compare")

Canbe used to check the str with the String to be compared







